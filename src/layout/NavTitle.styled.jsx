import styled from 'styled-components';

export const SNavTitle = styled.div`
  width: 100%;

  .nav-group-title {
    width: 100%;

    color: rgba(0, 0, 0, 0.7);

    font-size: 1.2rem;
    box-sizing: border-box;
    list-style: none;
    font-weight: 500;
    line-height: 48px;

    padding: 0 16px;
    margin: 0;

    white-space: nowrap;
  }

  .nav-group-evt-title {
    font-size: 1.2rem;
    font-weight: 100;

    margin-left: 5px;
  }
`;
